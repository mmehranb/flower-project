import { SafHtmlPipePipe } from './saf-html-pipe.pipe';

describe('SafHtmlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SafHtmlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
